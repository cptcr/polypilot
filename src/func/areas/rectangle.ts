/**
 * Calculates the area of a rectangle.
 * @param length - Length of the rectangle
 * @param width - Width of the rectangle
 * @returns - Total area of the rectangle
 */

export default async function rectangleArea(length: number, width: number): Promise<number> {
    return length * width;
}