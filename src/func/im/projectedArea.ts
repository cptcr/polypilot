/**
 * Calculates the projected area force requirements helper.
 * Just a simple area calc, but contextually named.
 */
export default function calculateProjectedArea(length: number, width: number): number {
    return length * width;
}