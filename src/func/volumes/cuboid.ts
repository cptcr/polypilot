/**
 * Calculates the volume of a cuboid.
 * @param length - Length
 * @param width - Width
 * @param height - Height
 * @returns - Volume
 */
export default function cuboidVolume(length: number, width: number, height: number): number {
    return length * width * height;
}